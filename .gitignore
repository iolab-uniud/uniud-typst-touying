# macOS
.DS_Store

# cartelle di lavoro dell'app Claude
Claude outputs/

dist/
.preview/
site/
.venv/

# I PDF sono output di compilazione: quelli degli esempi li costruisce la CI e
# finiscono fra gli allegati della release, non nel repository.
*.pdf

# configurazione locale dell'editor: solo quella del repository.
# La barra iniziale conta: senza, il pattern si applica a ogni livello e
# nasconderebbe anche `template/.vscode/`, che invece fa parte del pacchetto.
/.vscode/
